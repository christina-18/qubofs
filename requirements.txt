# Python dependencies for QUBO MS scRNA-seq pipeline.
# Python >= 3.10.  Install with:  pip install -r requirements.txt

# Core scientific stack
numpy>=1.24
scipy>=1.10                 # also provides scipy.io.mmread for pseudobulk .mtx files
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7

# QUBO solvers
dwave-neal>=0.6.0           # SimulatedAnnealingSampler (used in main pipeline)
dwave-samplers>=1.2.0       # TabuSampler (solver-independence validation)
# Optional: dwave-system    # only needed if running on D-Wave Leap quantum hardware
#                           # (Phase 2; see docs/DWAVE_SETUP.md)

# Utility
tqdm>=4.65
python-docx>=0.8.11         # build_manuscript_docx.py (manuscript .md → .docx)
