# Python dependencies for the qubofs MS CSF scRNA-seq benchmark.
# Python >= 3.10. Install with: pip install -r requirements.txt
# For exact reproducibility, use environment.yml or requirements-lock.txt.

# Core scientific stack
numpy>=1.24
scipy>=1.10                 # also provides scipy.io.mmread for pseudobulk .mtx files
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7

# QUBO solvers
dwave-neal>=0.6.0           # SimulatedAnnealingSampler used in the main pipeline
dwave-samplers>=1.2.0       # TabuSampler used for solver-sensitivity analysis

# Utility
tqdm>=4.65

# Optional, not used in the manuscript:
# dwave-system              # only needed for optional quantum-hardware experiments
